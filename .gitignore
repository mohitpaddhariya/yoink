# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# build artifacts
build/
dist/
*.egg-info/

# uv / virtualenv
.venv/

# OS / editor
.DS_Store

# secrets
.env

# Claude Code (local, per-machine)
.claude/settings.local.json
# internal planning / design docs — keep local, never publish
.claude/*.md

# benchmark run artifacts (machine-specific session ids + regenerable; numbers live in README + committed PNGs)
benchmark/results/
